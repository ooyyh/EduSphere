<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="top.ooyyh.edusphere.mapper.CategoryMapper">
    
    <!-- 分类结果映射 -->
    <resultMap id="CategoryResultMap" type="top.ooyyh.edusphere.entity.Category">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="slug" column="slug"/>
        <result property="description" column="description"/>
        <result property="icon" column="icon"/>
        <result property="color" column="color"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>
    
    <!-- 查询所有分类 -->
    <select id="getAllCategories" resultMap="CategoryResultMap">
        SELECT * FROM category 
        WHERE status = 1 
        ORDER BY sort_order ASC, created_at ASC
    </select>
    
    <!-- 根据ID查询分类 -->
    <select id="getCategoryById" resultMap="CategoryResultMap">
        SELECT * FROM category WHERE id = #{id}
    </select>
    
    <!-- 根据slug查询分类 -->
    <select id="getCategoryBySlug" resultMap="CategoryResultMap">
        SELECT * FROM category WHERE slug = #{slug}
    </select>
    
</mapper>

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="top.ooyyh.edusphere.mapper.CourseLessonMapper">
    
    <!-- 课时结果映射 -->
    <resultMap id="LessonResultMap" type="top.ooyyh.edusphere.entity.CourseLesson">
        <id property="id" column="id"/>
        <result property="sectionId" column="section_id"/>
        <result property="title" column="title"/>
        <result property="description" column="description"/>
        <result property="type" column="type"/>
        <result property="videoUrl" column="video_url"/>
        <result property="documentUrl" column="document_url"/>
        <result property="duration" column="duration"/>
        <result property="isFree" column="is_free"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>
    
    <!-- 根据章节ID查询课时 -->
    <select id="getLessonsBySectionId" resultMap="LessonResultMap">
        SELECT * FROM course_lesson 
        WHERE section_id = #{sectionId} 
        ORDER BY sort_order ASC, created_at ASC
    </select>
    
    <!-- 根据ID查询课时 -->
    <select id="getLessonById" resultMap="LessonResultMap">
        SELECT * FROM course_lesson WHERE id = #{id}
    </select>
    
    <!-- 插入课时 -->
    <insert id="insertLesson" parameterType="top.ooyyh.edusphere.entity.CourseLesson" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO course_lesson (section_id, title, description, type, video_url, document_url, duration, is_free, sort_order, created_at, updated_at)
        VALUES (#{sectionId}, #{title}, #{description}, #{type}, #{videoUrl}, #{documentUrl}, #{duration}, #{isFree}, #{sortOrder}, #{createdAt}, #{updatedAt})
    </insert>
    
    <!-- 更新课时 -->
    <update id="updateLesson" parameterType="top.ooyyh.edusphere.entity.CourseLesson">
        UPDATE course_lesson 
        SET title = #{title},
            description = #{description},
            type = #{type},
            video_url = #{videoUrl},
            document_url = #{documentUrl},
            duration = #{duration},
            is_free = #{isFree},
            sort_order = #{sortOrder},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>
    
    <!-- 删除课时 -->
    <delete id="deleteLesson">
        DELETE FROM course_lesson WHERE id = #{id}
    </delete>

    <!-- 根据章节ID获取课时数量 -->
    <select id="getLessonCountBySectionId" resultType="int">
        SELECT COUNT(*) FROM course_lesson WHERE section_id = #{sectionId}
    </select>

</mapper>
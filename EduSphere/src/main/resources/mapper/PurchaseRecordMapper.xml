<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.ooyyh.edusphere.mapper.PurchaseRecordMapper">

    <resultMap id="PurchaseRecordResultMap" type="top.ooyyh.edusphere.entity.PurchaseRecord">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="course_id" property="courseId"/>
        <result column="instructor_id" property="instructorId"/>
        <result column="purchase_price" property="purchasePrice"/>
        <result column="balance_before" property="balanceBefore"/>
        <result column="balance_after" property="balanceAfter"/>
        <result column="instructor_income" property="instructorIncome"/>
        <result column="platform_fee" property="platformFee"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <select id="getByUserId" resultMap="PurchaseRecordResultMap">
        SELECT pr.*, c.title as course_title, u.username as instructor_name
        FROM purchase_record pr
        LEFT JOIN course c ON pr.course_id = c.id
        LEFT JOIN user u ON pr.instructor_id = u.id
        WHERE pr.user_id = #{userId}
        ORDER BY pr.created_at DESC
    </select>

    <select id="getByInstructorId" resultMap="PurchaseRecordResultMap">
        SELECT pr.*, c.title as course_title, u.username as student_name
        FROM purchase_record pr
        LEFT JOIN course c ON pr.course_id = c.id
        LEFT JOIN user u ON pr.user_id = u.id
        WHERE pr.instructor_id = #{instructorId}
        ORDER BY pr.created_at DESC
    </select>

    <select id="getByUserIdAndCourseId" resultMap="PurchaseRecordResultMap">
        SELECT * FROM purchase_record 
        WHERE user_id = #{userId} AND course_id = #{courseId}
    </select>

    <select id="getByUserIdWithPage" resultMap="PurchaseRecordResultMap">
        SELECT pr.*, c.title as course_title, u.username as instructor_name
        FROM purchase_record pr
        LEFT JOIN course c ON pr.course_id = c.id
        LEFT JOIN user u ON pr.instructor_id = u.id
        WHERE pr.user_id = #{userId}
        ORDER BY pr.created_at DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countByUserId" resultType="int">
        SELECT COUNT(*) FROM purchase_record WHERE user_id = #{userId}
    </select>

    <select id="countByInstructorId" resultType="int">
        SELECT COUNT(*) FROM purchase_record WHERE instructor_id = #{instructorId}
    </select>

</mapper>
